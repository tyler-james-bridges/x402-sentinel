{
  "name": "@x402-sentinel/bundle-api",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'lint pending'",
    "test": "echo 'tests pending'",
    "dev": "node --watch src/index.ts"
  },
  "dependencies": {
    "@x402-sentinel/score": "workspace:*"
  }
}
